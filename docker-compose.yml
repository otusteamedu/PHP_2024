version: "3.3"

services:

  app-client:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    image: chat-app
    container_name: app-client
    volumes:
      - ./app:/data

  app-server:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    image: chat-app
    container_name: app-server
    volumes:
      - ./app:/data
