version: "3.3"

services:

  app:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    image: chat-app
    container_name: app
    hostname: app
    restart: always
    volumes:
      - ./app:/data
