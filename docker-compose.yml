version: "3.7"

services:
  db:
    image: postgres
    restart: always
    volumes:
      - ./data:/var/lib/postgresql/data
      - ./dump:/dump
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: example

