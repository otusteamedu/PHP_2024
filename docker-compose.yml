version: '3'

services:

  app:
    build:
      context: ./fpm
      dockerfile: Dockerfile
    image: otus/php
    container_name: app
    tty: true
    volumes:
       - ./app:/var/www/html
    networks:
      - app-network

networks:
  app-network:
    driver: bridge