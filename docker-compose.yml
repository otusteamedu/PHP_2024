version: "3.3"

services:

  app-client:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    image: chat-app
    container_name: app-client
    # hostname: app-client
    # restart: always
    volumes:
      - ./app:/data

  app-server:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    image: chat-app
    container_name: app-server
    # hostname: app-server
    # restart: always
    volumes:
      - ./app:/data
