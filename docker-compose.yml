version: '3'

services:
  nginx:
    container_name: nginx
    build: ./nginx
    ports:
      - 80:8080
    volumes:
      - ./data/www:/var/www/html
  php:
    container_name: php
    build: ./php
    volumes:
      - ./data/www:/var/www/html
  redis:
    container_name: redis
    image: redis
