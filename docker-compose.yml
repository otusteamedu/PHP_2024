services:
  php:
    image: php:8.2-cli
    container_name: php-container
    volumes:
      - .:/usr/src/myapp
    working_dir: /usr/src/myapp
    stdin_open: true # Открывает стандартный ввод для интерактивного режима
    tty: true        # Включает поддержку терминала
