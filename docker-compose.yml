version: '3'

services:
  app-server:
    build: docker/server
    volumes:
      - ./:/var/www
      - ./socket/:/var/run/socket
  app-client:
    build: docker/client
    volumes:
      - ./:/var/www
      - ./socket/:/var/run/socket
    depends_on:
      - app-server
