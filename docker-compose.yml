version: "3.8"

services:
  php-client:
    build:
      context: ./docker
      dockerfile: Dockerfile
    container_name: php-client
    volumes:
      - .:/data
    env_file: .env
    depends_on:
      - php-server
    networks:
      - app-network
  php-server:
    build:
      context: ./docker
      dockerfile: Dockerfile
    container_name: php-server
    volumes:
      - .:/data
    env_file: .env
    networks:
      - app-network
networks:
  app-network:
    driver: bridge