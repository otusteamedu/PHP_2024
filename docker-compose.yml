services:
  app:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    image: otus-shop/php
    container_name: app
    volumes:
      - ./code:/data/mysite.local
    networks:
      - app-network
    tty: true
    env_file: .env

networks:
  app-network:
    driver: bridge