version: '3'

services:
  php-fpm:
    build:
      context: docker/fpm
      dockerfile: Dockerfile
    image: app/php
    volumes:
      - ./:/data/myapp
    networks:
      - elastic

    working_dir: /data/myapp

networks:
  elastic:
    driver: bridge