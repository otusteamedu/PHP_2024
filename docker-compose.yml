version: '3'

services:
  app:
    build:
      context: ./docker/fpm
      dockerfile: Dockerfile
    image: otus-hw13/php
    container_name: app
    volumes:
      - ./app:/var/www
    networks:
      - otus-hw13-network

networks:
  otus-hw13-network:
    driver: bridge
