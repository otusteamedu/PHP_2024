version: "3.8"

services:
  db:
    image: postgres
    restart: always
    shm_size: 128mb
    volumes:
      - ./data:/var/lib/postgresql/data
      - ./dump:/dump
    environment:
      POSTGRES_PASSWORD: example

