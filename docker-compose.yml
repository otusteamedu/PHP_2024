services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_DB: cinema_db
      POSTGRES_USER: ali
      POSTGRES_PASSWORD: passwd
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
