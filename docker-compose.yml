version: '3'

services:
  app:
    build:
      context: ./docker/fpm
      dockerfile: Dockerfile
    image: otus-hw18/php
    container_name: app
    volumes:
      - ./app:/var/www
    networks:
      - otus-hw18-network

networks:
  otus-hw18-network:
    driver: bridge
