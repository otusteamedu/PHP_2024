#!/bin/bash

if ! [[ "$1" =~ ^-?[0-9]+(\.[0-9]+)?$ ]]; then
  echo "Введите корректоно первое число"
  exit 1
fi

if ! [[ "$2" =~ ^-?[0-9]+(\.[0-9]+)?$ ]]; then
  echo "Введите корректно второе число"
  exit 1
fi

result="$(awk "BEGIN {print $1 + $2}")"

echo "Сумма двух чисел равна: $result"