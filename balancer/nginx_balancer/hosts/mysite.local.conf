upstream nginx {
        least_conn;
        server nginx1:8080;
        server nginx2:8080;
}

server {
    listen 80;
    server_name mysite.local;

    location / {
        proxy_pass http://nginx;
    }
}
