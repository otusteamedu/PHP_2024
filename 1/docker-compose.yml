version: "3"

services:
  nginx:
    image: nginx:latest
    ports:
      - ${NGINX_PORT}:80
    volumes:
      - ./www:/app
      - ./docker/nginx/:/etc/nginx/conf.d/
    depends_on:
      - app
    networks:
      - net
    platform: linux/x86_64
  app:
    build: docker/php
    volumes:
      - ./www:/app
    networks:
      - net

networks:
  net:
    driver: bridge


