display_errors = On
log_errors = On
error_log = /var/log/php/errors.log

xdebug.mode = debug
xdebug.client_host = ${XDEBUG_REMOTE_IP}


; работают оба варианта хранения сессий.

;session.save_handler = memcached
;session.save_path = "hw5_memcached:11211"

session.save_handler = redis
session.save_path = "tcp://hw5_redis:6379"
