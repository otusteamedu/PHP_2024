create table users(
                      id serial primary key,
                      name varchar not null,
                      last_name varchar,
                      phone varchar,
                      email varchar
);
