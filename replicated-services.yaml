version: '3'
services:
  app:
    build: .
    volumes:
      - .:/data/mysite.local

  nginx:
    image: nginx:stable
    volumes:
      - .:/data/mysite.local
      - ./nginx/hosts/mysite.local.conf:/etc/nginx/conf.d/mysite.local.conf
