version: '3'

services:

  server:
    build:
      context: ./php
      dockerfile: Dockerfile
    image: php
    container_name: server
    volumes:
      - ./code:/data

  client:
    build:
      context: ./php
      dockerfile: Dockerfile
    image: php
    container_name: client
    volumes:
      - ./code:/data
