services:
  server:
    build:
      context: ./docker
      dockerfile: Dockerfile
    image: data/server
    container_name: server
    volumes:
      - ./code:/code
    networks:
      - app-network

  client:
    build:
      context: ./docker
      dockerfile: Dockerfile
    image: data/client
    container_name: client
    volumes:
      - ./code:/code
    depends_on:
      - server
    networks:
      - app-network

networks:
  app-network:
    driver: bridge