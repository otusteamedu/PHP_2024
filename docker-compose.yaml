version: '3.8'

services:
  server:
    container_name: server
    build:
      context: ./docker
      dockerfile: Dockerfile
    volumes:
      - ./code:/var/www/html
      - /tmp/sockets:/tmp/sockets

  client:
    container_name: client
    build:
      context: ./docker
      dockerfile: Dockerfile
    volumes:
      - ./code:/var/www/html
      - /tmp/sockets:/tmp/sockets