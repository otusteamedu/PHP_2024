# версия синтаксиса
version: '3'

networks:
  app-network:
    driver: bridge
services:
  verification:
    build:
      context: docker/php
      dockerfile: Dockerfile
    image: verification/php
    container_name: verification
    volumes:
      - ./code:/data/mysite.local
    env_file: .env
    networks:
      - app-network





