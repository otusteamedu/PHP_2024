upstream nginx-backend {
    least_conn;
    server nginx-server-1:80;
    server nginx-server-2:80;
}

server {
    listen 80;

    server_name mysite.local;

    location / {
        proxy_pass http://nginx-backend;
    }
}
