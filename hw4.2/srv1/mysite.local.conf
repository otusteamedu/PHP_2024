server {
    listen 8087;
    # нужно указать, какому доменному имени принадлежит наш конфиг
    #server_name srv1.mysite.local;

    # задаём корневую директорию
    root /data/mysite.local;

    # стартовый файл
    index index.html;
}
