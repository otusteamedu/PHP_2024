upstream webservers {
    least_conn;
    server webserver1:8080;
    server webserver2:8080;
}

server {
    listen 80 default_server;
    
    location / {
        proxy_pass http://webservers;
    }
}
