upstream nginx-backend {
    least_conn;
    server nginx1:80;
    server nginx2:80;
}

server {
    listen 80;
    server_name mysite.local;
    #root /data/mysite.local;
    #index index.php;
    
    location / {
        proxy_pass http://nginx-backend;
    }
}