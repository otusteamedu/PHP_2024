upstream nginx-balancer {
    least_conn;
    server nginx1:80;
    server nginx2:80;
}

server {
    listen 80;
    server_name ira.yu;
    location / {
        proxy_pass http://nginx-balancer;
        proxy_set_header   Host  $host;
    }
}