<?php

class ProductChecker {
    public function check(ProductInterface $product): bool {
        // Реализация проверки, возвращает true, если продукт соответствует стандартам
        return true;
    }
}
