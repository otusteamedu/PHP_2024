upstream nginx-upstream {
    least_conn;
    server nginx1:80;
    server nginx2:80;
}

server {
    listen 80;
    server_name application.local;

    location / {
        proxy_pass http://nginx-upstream;
    }
}
