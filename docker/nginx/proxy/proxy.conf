upstream nginx-proxy {
    least_conn;
    server nginx:80;
    server nginx2:80;
}

server {
    listen 80;

    server_name mysite.local;

	location / {
	    proxy_pass http://nginx-proxy;
	    proxy_set_header   Host  $host;
	}
}
