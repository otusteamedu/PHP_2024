upstream nginx-backend {
        least_conn;
        server nginx_1:80;
        server nginx_2:80;
        server nginx_3:80;
    }

    server {
        listen 80;
        server_name application.local;
        location / {
            proxy_pass http://nginx-backend;
        }
    }