
    upstream balancer {
        least_conn;
        server nginx_instance1;
        server nginx_instance2;
        server nginx_instance3;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://balancer;
        }
    }
